version: "3"

services:
  mysql:
    image: mysql:5.7
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: dev
      MYSQL_ROOT_PASSWORD: root

    networks:
      - net


networks:
  net:
    driver: bridge